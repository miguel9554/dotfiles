#!/bin/sh
# TODO zip in remote dir, scp the ziped file, unzip file, remove zip from server

REMOTE_DIR=/home/miguel/backup/database/postgresql/
BACKUPS_DIR=/mnt/data/backups/opiniones

scp -r miguel@proyectoingenieria.com.ar:${REMOTE_DIR} ${BACKUPS_DIR}
